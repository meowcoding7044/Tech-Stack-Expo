# iOS App Distribution Guide 📱🚀

คู่มือการสร้าง Certificate, Provisioning Profile และการแจกจ่าย iOS App ในรูปแบบต่างๆ

## 📋 Overview Workflow

```mermaid
graph TD
    A[Start: Generate CSR on Mac] --> B[Apple Developer Portal]
    B --> C[Create Certificate (.cer)]
    B --> D[Create App ID (Identifier)]
    C --> E[Create Provisioning Profile]
    D --> E
    E --> F[Build App (.ipa)]
    F --> G{Distribution Method}
    G -->|App Store / TestFlight| H[Upload via Transporter/Xcode]
    G -->|Ad Hoc / Enterprise| I[OTA Distribution (Self-hosted)]
```

---

## 1. การเตรียม Certificates & Profiles (Preparation) 🔑

ก่อนเริ่ม Build แอป คุณต้องเตรียมกุญแจและสิทธิ์ต่างๆ ให้เรียบร้อยก่อน

### 1.1 สร้างไฟล์ CSR (Certificate Signing Request)
ทำที่เครื่อง Mac ของคุณ:
1. เปิดโปรแกรม **Keychain Access**
2. เมนู **Keychain Access > Certificate Assistant > Request a Certificate from a Certificate Authority...**
3. กรอกข้อมูล:
   - **User Email Address**: อีเมล Apple ID ของคุณ
   - **Common Name**: ชื่อของคุณ (ภาษาอังกฤษ)
   - **Request is**: เลือก **Saved to disk**
4. กด **Continue** จะได้ไฟล์ `.certSigningRequest` (เก็บไว้ให้ดี)

### 1.2 สร้าง Certificate บน Apple Developer Portal
1. ไปที่ [Apple Developer Portal (Certificates)](https://developer.apple.com/account/resources/certificates/list)
2. กดปุ่ม `+` เพื่อสร้างใหม่
3. **เลือกประเภท Certificate**:
   - 🛠 **Apple Development**: สำหรับทดสอบตอนเขียนโค้ด (Debug)
   - 🚀 **Apple Distribution**: สำหรับส่ง App Store, TestFlight หรือ Ad Hoc/Enterprise
4. Upload ไฟล์ CSR ที่ได้จากข้อ 1.1
5. กด **Download** จะได้ไฟล์ `.cer` (ดับเบิ้ลคลิกเพื่อติดตั้งลงเครื่อง)

### 1.3 สร้าง Identifiers & Profiles
เพื่อให้ Build ผ่าน ต้องจับคู่ App ID กับ Certificate เข้าด้วยกัน

1. **Identifiers (App ID)**:
   - ไปที่เมนู **Identifiers** กด `+`
   - Register **App ID** โดยใส่ **Bundle ID** ให้ตรงกับโปรเจกต์ (เช่น `com.company.appname`)

2. **Profiles (Provisioning Profile)**:
   - ไปที่เมนู **Profiles** กด `+`
   - เลือกประเภทให้ตรงกับงาน:
     - **App Store**: ส่งขึ้น Store / TestFlight
     - **Ad Hoc**: ติดตั้งในเครื่องที่ลงทะเบียน UDID ไว้
     - **In-House** (Enterprise Only): ติดตั้งได้ทุกเครื่องในองค์กร 🏢
   - เลือก App ID และ Certificate ที่สร้างไว้
   - ตั้งชื่อ Profile และ Download ไฟล์ `.mobileprovision`

---

## 2. การ Build (Expo EAS) 🛠️

การ Build ผ่าน Expo Application Services (EAS) ทำให้ชีวิตง่ายขึ้นมาก

### ตัวอย่าง `eas.json` สำหรับ Development/Internal
สำหรับการแจกจ่ายภายใน (Internal Distribution) เรามักใช้ profile แบบ `internal` (Ad Hoc / Enterprise)

```json
{
  "build": {
    "dev": {
      "distribution": "internal",
      "ios": {
        "resourceClass": "m1-medium"
      },
      "env": {
        "APP_ENV": "dev"
      }
    },
    "production": {
      "distribution": "store",
      "autoIncrement": true
    }
  }
}
```

### คำสั่ง Build
- **สำหรับแจกจ่ายภายใน (Ad Hoc / Enterprise)**:
  ```bash
  eas build --platform ios --profile dev
  ```
- **สำหรับส่ง App Store**:
  ```bash
  eas build --platform ios --profile production
  ```

---

## 3. วิธีการแจกจ่าย (Distribution Methods) 📦

### 3.1 TestFlight (App Store Connect)
✅ **วิธีมาตรฐานและง่ายที่สุด**
1. Upload ไฟล์ `.ipa` ผ่าน Transporter หรือ EAS Submit
2. เข้าไปที่ **App Store Connect** > **TestFlight**
3. เพิ่ม Email ของ User
4. User จะได้รับอีเมลเชิญให้โหลดผ่านแอป **TestFlight**

### 3.2 Ad Hoc Distribution
🔒 **จำกัดเครื่อง**
- เหมาะสำหรับการเทสกลุ่มเล็กๆ ที่ไม่ได้ใช้ TestFlight
- **ข้อจำกัด**: ติดตั้งได้เฉพาะเครื่องที่มี **UDID** ลงทะเบียนไว้ใน Provisioning Profile เท่านั้น

### 3.3 Enterprise / In-House Distribution
🏢 **สำหรับการใช้งานภายในองค์กร (ไม่ต้องผ่าน App Store)**
- ต้องใช้ **Apple Developer Enterprise Program** ($299/ปี)
- **ข้อดี**: ไม่จำกัดจำนวนเครื่อง, ไม่ต้องเก็บ UDID
- **วิธีการ**: ใช้ **OTA (Over-the-Air) Manifest**

#### ขั้นตอนการทำ OTA Distribution (Self-Hosted)
ต้องมี 3 สิ่งวางอยู่บน Server เดียวกัน (และต้องเป็น **HTTPS**):
1. **ไฟล์ App**: `app.ipa`
2. **รูปภาพ**: `icon.png` (57x57), `icon-full.png` (512x512)
3. **ไฟล์ Manifest**: `manifest.plist`

**ตัวอย่างไฟล์ `manifest.plist`:**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>items</key>
    <array>
        <dict>
            <key>assets</key>
            <array>
                <dict>
                    <key>kind</key>
                    <string>software-package</string>
                    <key>url</key>
                    <!-- [1] ลิงก์ตรงไปที่ไฟล์ .ipa (ต้องเป็น HTTPS) -->
                    <string>https://your-server.com/path/to/app.ipa</string>
                </dict>
            </array>
            <key>metadata</key>
            <dict>
                <key>bundle-identifier</key>
                <!-- [2] Bundle ID ต้องตรงกับใน App -->
                <string>com.yourcompany.appname</string>
                <key>bundle-version</key>
                <string>1.0.0</string>
                <key>kind</key>
                <string>software</string>
                <key>title</key>
                <!-- [3] ชื่อที่จะแสดงตอน pop-up ถามติดตั้ง -->
                <string>My App Name</string>
            </dict>
        </dict>
    </array>
</dict>
</plist>
```

**สร้างลิงก์ดาวน์โหลดบนหน้าเว็บ:**
Browser บน iOS จะไม่โหลด `.ipa` ตรงๆ ต้องเรียกผ่านโปรโตคอล `itms-services`

```html
<!-- ลิงก์ต้องชี้ไปที่ไฟล์ .plist ไม่ใช่ .ipa -->
<a href="itms-services://?action=download-manifest&url=https://your-server.com/path/to/manifest.plist">
   📲 ติดตั้งแอปพลิเคชัน
</a>
```

#### การติดตั้งครั้งแรก (Untrusted Enterprise Developer)
เมื่อ User ติดตั้งเสร็จ จะยังเข้าแอปไม่ได้ในครั้งแรก ต้องไปกดเชื่อถือผู้พัฒนาก่อน:
1. ไปที่ **Settings** > **General** > **VPN & Device Management**
2. เลือกชื่อบริษัท (Enterprise Profile)
3. กด **Trust "..."**

#### 💡 ทางเลือก: ใช้บริการ Third-Party (ง่ายกว่ามาก)
หากคุณไม่มี Server ของตัวเอง หรือไม่อยากยุ่งยากเรื่องการสร้าง `manifest.plist` แนะนำให้ใช้บริการอย่าง:
- **[Diawi](https://www.diawi.com/)**
- **[InstallOnAir](https://www.installonair.com/)**

**ข้อดี**: ระบบพวกนี้จะสร้าง Manifest และหน้าดาวน์โหลดให้คุณโดยอัตโนมัติ เพียงแค่ลากไฟล์ `.ipa` ไปวาง ก็จะได้ลิงก์ส่งให้ทีมทดสอบได้ทันทีครับ! 🚀

---

## ⚠️ ข้อควรระวัง (Important Notes)

> [!WARNING]
> **Certificate Expiry (หมดอายุ)**
> - iOS Distribution Certificate **มีอายุ 1 ปี**
> - เมื่อหมดอายุ **แอปที่ติดตั้งไปแล้วจะเปิดไม่ได้ทันที** (Crash/เด้งออก)
> - ต้องทำการ Renew Certificate, Re-build แอป และให้ User ติดตั้งใหม่ทุกปี

> [!TIP]
> **HTTPS Required**
> Apple บังคับว่า Server ที่เก็บไฟล์ `.plist` และ `.ipa` ต้องรองรับ **HTTPS** ที่ถูกต้องเท่านั้น (Self-signed certificate มักจะใช้ไม่ได้)

> [!NOTE]
> **MIME Types**
> หากคลิกลิงก์แล้วไม่มีอะไรเกิดขึ้น ให้เช็ค Server Config ว่ารองรับ MIME type:
> - `.ipa` -> `application/octet-stream`
> - `.plist` -> `text/xml` หรือ `application/xml`
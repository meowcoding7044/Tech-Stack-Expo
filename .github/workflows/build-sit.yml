name: Build SIT Android

on:
  push:
    branches: [develop]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: |
          npm install -g expo-cli eas-cli
          npm install

      - name: Build with EAS
        run: |
          eas build -p android --profile sit --non-interactive --json > build.json
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Extract download URL
        run: |
          echo "BUILD_URL=$(jq -r '.[0].artifacts.buildUrl' build.json)" >> $GITHUB_ENV

      - name: Send Email to Tester
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_HOST }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USER }}
          password: ${{ secrets.SMTP_PASS }}
          subject: "📦 SIT Build Ready"
          to: ${{ secrets.MAIL_TO }}
          from: Expo CI <${{ secrets.SMTP_USER }}>
          html_body: |
            <h3>SIT Build สำเร็จ ✅</h3>
            <p>ดาวน์โหลด APK:</p>
            <a href="${{ env.BUILD_URL }}">👉 Click to download</a>
            <p><b>Branch:</b> develop</p>
            <p><b>Commit:</b> ${{ github.sha }}</p>
